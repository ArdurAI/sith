apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "sith-hub.fullname" . }}
  labels:
    {{- include "sith-hub.labels" . | nindent 4 }}
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    resourceNames: ["sith-reader"]
    verbs: ["get"]
